# 生产环境配置示例（后端）
# 将以下内容粘贴到 Render 后端服务的 Environment 中，或复制为 .env.production（仅本地，不提交）

# 基础
NODE_ENV=production
PORT=10000

# 安全
# 使用随机且足够长度的强密码（例如 32+ 字符）
JWT_SECRET=change_me_to_a_strong_secret

# 数据库
DB_TYPE=mongodb              # 可选: mongodb | firebase
DB_DUAL_WRITE=false          # 双写（迁移/同步时才开启）
READ_PRIMARY=mongodb         # 双写时读取主库

# MongoDB Atlas 连接串（请替换用户名/密码/集群）
MONGODB_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/tripwage?retryWrites=true&w=majority

# Firebase（仅使用 Firebase 时需要；服务端推荐使用以下两种之一）
# 方式 1：使用服务账号 JSON 字符串（多行需转义）
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your_project_id",...}
# 方式 2：分开提供字段
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# CORS
CORS_ORIGIN=https://your-frontend-domain.com

# 其他
LOG_LEVEL=info
